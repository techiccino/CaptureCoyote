Drop branding assets here to light up the shell automatically.

Expected files:

- `CaptureCoyoteLogo.png`
- `CaptureCoyote.ico`

Notes:

- The PNG is used in the main window, settings window, review window, and editor header.
- The ICO is used for the window icon, taskbar icon, and the packaged app icon when present during build.
- Recommended PNG size: at least `512x512` with transparency.
- Recommended ICO sizes: include `16`, `24`, `32`, `48`, `64`, and `256` pixel variants.
