<Window x:Class="CaptureCoyote.Editor.Views.DetectedTextWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detected Text"
        Width="980"
        Height="760"
        MinWidth="760"
        MinHeight="520"
        Background="#FF0C1118"
        Foreground="White"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#FF1884C4" />
            <Setter Property="BorderBrush" Value="#551884C4" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF2397DA" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF1172AB" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#77FFFFFF" />
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1C2530" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#22303C49" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Gesture="Ctrl+C" Command="{Binding CopyDetectedTextCommand}" />
        <KeyBinding Gesture="Ctrl+Shift+C" Command="{Binding CopySearchableTextCommand}" />
    </Window.InputBindings>

    <Border Margin="18"
            Padding="18"
            Background="#FF111821"
            BorderBrush="#331884C4"
            BorderThickness="1"
            CornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DockPanel LastChildFill="False">
                <StackPanel DockPanel.Dock="Left"
                            Orientation="Horizontal">
                    <Border x:Name="BrandLogoContainer"
                            Width="42"
                            Height="42"
                            Margin="0,0,12,0"
                            Padding="4"
                            Background="#1A1884C4"
                            CornerRadius="14"
                            Visibility="Collapsed">
                        <Image x:Name="BrandLogoImage" Stretch="Uniform" Visibility="Collapsed" />
                    </Border>
                    <StackPanel>
                        <TextBlock FontSize="22"
                                   FontWeight="SemiBold"
                                   Text="OCR Review" />
                        <TextBlock Margin="0,4,0,0"
                                   Foreground="#99FFFFFF"
                                   Text="Review OCR, annotation text, and the combined searchable text stored in this project." />
                    </StackPanel>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right"
                            Orientation="Horizontal">
                    <Button Style="{StaticResource ActionButtonStyle}"
                            Command="{Binding CopyDetectedTextCommand}"
                            Content="Copy OCR Text" />
                    <Button Style="{StaticResource ActionButtonStyle}"
                            Command="{Binding CopySearchableTextCommand}"
                            Content="Copy Search Index" />
                    <Button Style="{StaticResource ActionButtonStyle}"
                            Margin="0"
                            Padding="14,9"
                            Background="#1C2530"
                            BorderBrush="#22303C49"
                            Click="CloseButton_OnClick"
                            Content="Close" />
                </StackPanel>
            </DockPanel>

            <Border Grid.Row="1"
                    Margin="0,16,0,0"
                    Padding="12"
                    Background="#99111821"
                    BorderBrush="#221884C4"
                    BorderThickness="1"
                    CornerRadius="14">
                <StackPanel>
                    <TextBlock FontWeight="SemiBold"
                               Foreground="#FFF2F6FA"
                               Text="{Binding OcrReviewSummary}" />
                    <TextBlock Margin="0,4,0,0"
                               Foreground="#88FFFFFF"
                               Text="{Binding OcrGuidanceText}"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Grid Grid.Row="2"
                    Margin="0,18,0,18"
                  >
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Padding="12"
                        Background="#FF0E141C"
                        BorderBrush="#221884C4"
                        BorderThickness="1"
                        CornerRadius="16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock FontWeight="SemiBold" Text="OCR Text" />
                        <TextBox x:Name="DetectedTextTextBox"
                                 Grid.Row="1"
                                 Margin="0,10,0,0"
                                 Padding="0,0,10,0"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="#FFF2F6FA"
                                 FontFamily="Consolas"
                                 FontSize="13"
                                 IsReadOnly="True"
                                 AcceptsReturn="True"
                                 Text="{Binding ExtractedText, Mode=OneWay}"
                                 TextWrapping="Wrap"
                                 VerticalContentAlignment="Top"
                                 VerticalAlignment="Stretch"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Disabled" />
                    </Grid>
                </Border>

                <Grid Grid.Row="1" Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Padding="12"
                            Background="#FF0E141C"
                            BorderBrush="#221884C4"
                            BorderThickness="1"
                            CornerRadius="16">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <TextBlock FontWeight="SemiBold" Text="Annotation Text" />
                            <TextBox Grid.Row="1"
                                     Margin="0,10,0,0"
                                     Padding="0,0,10,0"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="#FFF2F6FA"
                                     FontFamily="Consolas"
                                     FontSize="12.5"
                                     IsReadOnly="True"
                                     AcceptsReturn="True"
                                     Text="{Binding AnnotationText, Mode=OneWay}"
                                     TextWrapping="Wrap"
                                     VerticalContentAlignment="Top"
                                     VerticalAlignment="Stretch"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Disabled" />
                        </Grid>
                    </Border>

                    <Border Grid.Column="1"
                            Margin="14,0,0,0"
                            Padding="12"
                            Background="#FF0E141C"
                            BorderBrush="#221884C4"
                            BorderThickness="1"
                            CornerRadius="16">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <TextBlock FontWeight="SemiBold" Text="{Binding SearchIndexHeading}" />
                            <TextBox Grid.Row="1"
                                     Margin="0,10,0,0"
                                     Padding="0,0,10,0"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="#FFF2F6FA"
                                     FontFamily="Consolas"
                                     FontSize="12.5"
                                     IsReadOnly="True"
                                     AcceptsReturn="True"
                                     Text="{Binding CombinedSearchText, Mode=OneWay}"
                                     TextWrapping="Wrap"
                                     VerticalContentAlignment="Top"
                                     VerticalAlignment="Stretch"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Disabled" />
                        </Grid>
                    </Border>
                </Grid>
            </Grid>

            <TextBlock Grid.Row="3"
                       Foreground="#77FFFFFF"
                       Text="Tip: Ctrl+C copies OCR text, and Ctrl+Shift+C copies the search index used for OCR plus annotation text." />
        </Grid>
    </Border>
</Window>
